import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sfon.test_app',
  appName: 'SFON',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
